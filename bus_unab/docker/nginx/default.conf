upstream php-fpm {
    server app:9000;
    keepalive 32;
}

# ── HTTP  →  HTTPS redirect ────────────────────────────────────────────────────
# Descomenta este bloque DESPUÉS de obtener el certificado SSL
# server {
#     listen 80;
#     server_name bus.unab.edu.co;
#
#     location /.well-known/acme-challenge/ {
#         root /var/www/certbot;
#     }
#
#     location / {
#         return 301 https://$host$request_uri;
#     }
# }

# ── HTTP server (activo hasta configurar SSL) ──────────────────────────────────
server {
    listen 80;
    server_name _;

    root  /var/www/html/public;
    index index.php;

    charset utf-8;

    # Let's Encrypt challenge
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # Security headers
    add_header X-Frame-Options        "SAMEORIGIN"                    always;
    add_header X-Content-Type-Options "nosniff"                       always;
    add_header X-XSS-Protection       "1; mode=block"                 always;
    add_header Referrer-Policy        "strict-origin-when-cross-origin" always;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location = /favicon.ico { access_log off; log_not_found off; }
    location = /robots.txt  { access_log off; log_not_found off; }

    error_page 404 /index.php;

    location ~ \.php$ {
        fastcgi_pass            php-fpm;
        fastcgi_index           index.php;
        fastcgi_param           SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
        include                 fastcgi_params;
        fastcgi_read_timeout    300;
        fastcgi_buffer_size     64k;
        fastcgi_buffers         4 64k;
        fastcgi_busy_buffers_size 128k;
        fastcgi_param           HTTP_X_FORWARDED_PROTO $scheme;
    }

    # Bloquear acceso a archivos ocultos
    location ~ /\.(?!well-known) {
        deny all;
    }

    # Cache agresivo para assets compilados de Vite (contenido hasheado)
    location /build/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        access_log off;
    }

    location ~* \.(jpg|jpeg|png|gif|ico|svg|webp|woff|woff2|ttf|eot|pdf)$ {
        expires 30d;
        add_header Cache-Control "public";
        access_log off;
    }
}

# ── HTTPS server ───────────────────────────────────────────────────────────────
# Descomenta este bloque DESPUÉS de obtener el certificado y editar el dominio
#
# server {
#     listen 443 ssl;
#     http2  on;
#     server_name bus.unab.edu.co;
#
#     root  /var/www/html/public;
#     index index.php;
#
#     # Certificados Let's Encrypt
#     ssl_certificate     /etc/letsencrypt/live/bus.unab.edu.co/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/bus.unab.edu.co/privkey.pem;
#     ssl_trusted_certificate /etc/letsencrypt/live/bus.unab.edu.co/chain.pem;
#
#     ssl_protocols       TLSv1.2 TLSv1.3;
#     ssl_ciphers         ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305;
#     ssl_prefer_server_ciphers off;
#     ssl_session_cache   shared:SSL:10m;
#     ssl_session_timeout 1d;
#     ssl_stapling        on;
#     ssl_stapling_verify on;
#
#     add_header Strict-Transport-Security "max-age=63072000; includeSubDomains; preload" always;
#     add_header X-Frame-Options           "SAMEORIGIN"                                   always;
#     add_header X-Content-Type-Options    "nosniff"                                      always;
#     add_header X-XSS-Protection          "1; mode=block"                                always;
#     add_header Referrer-Policy           "strict-origin-when-cross-origin"              always;
#
#     location / {
#         try_files $uri $uri/ /index.php?$query_string;
#     }
#
#     location = /favicon.ico { access_log off; log_not_found off; }
#     location = /robots.txt  { access_log off; log_not_found off; }
#     error_page 404 /index.php;
#
#     location ~ \.php$ {
#         fastcgi_pass            php-fpm;
#         fastcgi_index           index.php;
#         fastcgi_param           SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
#         include                 fastcgi_params;
#         fastcgi_read_timeout    300;
#         fastcgi_buffer_size     64k;
#         fastcgi_buffers         4 64k;
#         fastcgi_param           HTTP_X_FORWARDED_PROTO https;
#     }
#
#     location ~ /\.(?!well-known) { deny all; }
#
#     location /build/ {
#         expires 1y;
#         add_header Cache-Control "public, immutable";
#         access_log off;
#     }
#
#     location ~* \.(jpg|jpeg|png|gif|ico|svg|webp|woff|woff2|ttf|eot|pdf)$ {
#         expires 30d;
#         add_header Cache-Control "public";
#         access_log off;
#     }
# }
